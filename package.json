{
  "name": "coca_site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite src --port 3000",
    "build": "vite build src",
    "preview": "vite preview src --port 3000"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  }
}
